# Contract guidance

- Contract files describe implemented behavior, not planned behavior.
- Changes require review from the API owner and every affected client owner.
- Prefer additive changes. Mark and schedule breaking changes explicitly before implementation.
- Keep `openapi.yaml`, `error-codes.md`, `role-permissions.md`, API tests and consumer code synchronized.
